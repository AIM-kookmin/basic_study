# 실행 환경과 수업 전 준비

## Google Colab

1. Colab에서 해당 주차 ipynb를 업로드합니다.
2. 런타임 유형에서 GPU를 선택할 수 있습니다. 기본 실습은 CPU도 지원합니다.
3. 환경 셀을 실행하여 torch, torchvision, device를 확인합니다. Colab에 설치된 두 패키지를 우선 사용합니다.
4. 이미지 주차는 데이터 셀까지 수업 전에 실행합니다. FashionMNIST는 압축 약 30MB, CIFAR10은 약 170MB, MNIST는 약 12MB이며 압축 해제 공간도 필요합니다.
5. 실행 결과는 `파일 → 다운로드 → .ipynb 다운로드`로 보관합니다. `/content/outputs`의 모델·JSON은 필요하면 별도로 다운로드합니다.

각 노트북은 독립 실행 가능합니다. 단, Colab 런타임을 초기화하면 다운로드와 학습 결과가 사라집니다.
서로 다른 노트북을 같은 런타임에서 번갈아 실행했다면 새 런타임에서 위부터 실행하여 전역 변수 충돌을 피하세요.

## 로컬 Windows / CPU

Python 3.10 이상을 권장하며 실제 검증 버전은 검증 보고서에 기록합니다.
아래 명령은 자료 폴더에서 실행합니다. 가상환경을 써서 기존 설치와 분리하세요.

```powershell
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install --upgrade pip
.\.venv\Scripts\python.exe -m pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
.\.venv\Scripts\python.exe -m pip install notebook ipykernel numpy matplotlib
.\.venv\Scripts\python.exe -m ipykernel install --user --name aim-study --display-name "AIM Study"
.\.venv\Scripts\python.exe -m notebook
```

Jupyter에서 `AIM Study` 커널을 선택합니다. GPU 로컬 설치는 PyTorch 공식 설치 페이지에서 자신의 CUDA 환경에 맞는 명령을 사용하세요.
torch와 torchvision의 임의 버전을 따로 섞지 말고 호환되는 조합으로 설치합니다.

## QUICK와 학습 예산

| 설정 | 기본값 | 확대 방법 |
|---|---|---|
| 이미지 train | 4,096개 | `QUICK=False` 또는 `TRAIN_N` 수정 |
| 이미지 val/test | 각각 1,024개 | val 최대 5,000 / test 최대 10,000 |
| CNN 학습 | 3 epoch | `EPOCHS` 증가 |
| VAE 학습 | 5주차 10 / 6주차 β 비교 8 / CVAE 20 epoch | `EPOCHS`, `CVAE_EPOCHS` 조절 |
| 문자 모델 | context 32, batch 32, 240 steps | `STEPS` 증가, 비교 목적에 맞춰 context 조절 |
| VAE | week5 Z=2 / week6 β 비교 Z=8, CVAE Z=2 | latent 차원은 실험 변수로 변경 |

실험 변수를 바꾼 뒤에는 **모델과 optimizer를 새로 만들고** 비교합니다. cell의 함수 기본 인자는 정의 시점 값으로 고정되므로 설정 변경 후 관련 정의 셀부터 다시 실행하세요.
`SMOKE`는 제작 검증용 환경변수이며 수강생의 품질 비교에는 사용하지 않습니다.

## 자주 발생하는 문제

| 증상 | 확인할 점 / 해결 |
|---|---|
| torch·torchvision import 오류 | Colab 새 런타임 또는 로컬의 호환 버전 조합 확인 |
| 데이터 다운로드가 오래 걸림 | 최초 파일 다운로드·압축 해제 대기. 연결 상태 확인 후 재실행. torchvision이 무결성 검사를 수행함 |
| `expected ... channels` | 이미지 shape의 C와 첫 Conv의 in_channels 일치 여부 |
| `mat1 and mat2 shapes` | Flatten 직전 shape와 Linear in_features 계산 |
| device mismatch | model, input, target, 새 mask/position 텐서의 device 확인 |
| label dtype 오류 | cross entropy target은 정수 `torch.long` |
| loss가 NaN | 범위·lr·logvar exp·모두 차단된 attention 행 확인 |
| VAE target 범위 오류 | MNIST를 0~1로 유지. CIFAR의 [-1,1] Normalize를 VAE에 재사용하지 않음 |
| GPU 메모리 부족 | BATCH를 먼저 줄이고, LM은 CONTEXT·D·layers도 조절 |
| 생성 문자열 품질이 낮음 | 작은 모델·합성 코퍼스·짧은 학습의 한계. 올바른 loss·causality 검사를 먼저 확인 |
| loss는 낮은데 생성이 이상함 | 미래 정보 누출, train/eval 모드, tokenizer와 checkpoint 설정 확인 |

## 자료 재생성 (리더만)

`python-pptx`, `nbformat`을 설치한 환경에서 `_제작소스/build_slides.py`, `build_notebooks.py`, `build_homework.py`를 순서대로 실행하면 파일을 재생성합니다.
기존 동일 이름의 생성물과 저장된 노트북 출력이 갱신되므로 수정·실행 결과는 먼저 다른 이름으로 보관하세요.
`render_slides.py`는 Windows PowerPoint 설치가 필요하며 PDF와 검사 미리보기를 생성합니다.
`validate_notebooks.py`는 제작자 로컬 검증 경로를 사용하므로 다른 PC에서는 환경변수 `AIM_DATA_ROOT`를 자신의 데이터 경로로 설정하세요.
